<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
    <form action="/login" method="POST">
      <% if(!username) { %>
        <input type="text" name="username" placeholder="Username" style="width: 300px; margin: 1em">
        <button class="btn btn-outline-dark" type="submit">Log In</button>  
      <%}%>
    </form>
    <form action="/logout" method="POST">
      <% if(username) { %>
        <p style="display: inline-block; margin-right: 5px; margin-top:10px;">Logged In As: <%= username.email %>! </p>
        <button class="btn btn-outline-dark" type=“submit”>Logout</button>
      <%}%>
    </form>
  </nav>
</header>